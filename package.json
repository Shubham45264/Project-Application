{
  "name": "project-camp-root",
  "private": true,
  "scripts": {
    "build": "cd \"Project Camp Frontend\" && npm install && npm run build",
    "install-all": "npm install && cd \"Project Camp Backend\" && npm install && cd \"../Project Camp Frontend\" && npm install"
  }
}
